'''
Problem Statement:

Develop a Python program that takes a customer's name, the number of items purchased, and the total amount before tax. The program should:

Calculate the total cost after adding GST (18%).

Check if the order qualifies for free shipping (orders above ₹1000).

Display the order summary with all details.
'''

# Online Shopping Order Processor
customer_name = input("Enter customer name: ")
number_of_items = int(input("Enter number of items ordered: "))
total_price_before_tax=float(input("Enter total price before tax: "))

#Calculate tax and total cost
gst_rate=18/100 #18% GST
tax_amt=total_price_before_tax*gst_rate
total_price_after_tax=total_price_before_tax+tax_amt

#Determine free shipping eligibility
free_shipping =total_price_after_tax>1000

#Display order summary
print("\n========= Order Summary ========")   
print(f"Customer Name: {customer_name}")
print(f"Number of Items Ordered: {number_of_items}")
print(f"Total Price before Tax: ₹{total_price_before_tax:.2f}")
print(f"GST (18%): ₹{tax_amt:.2f}")
print(f"Total Price after Tax: ₹{total_price_after_tax:.2f}")
print(f"Eligible for Free Shipping: {'Yes' if free_shipping else 'No'}")

 
    
    
                             
